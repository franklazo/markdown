# Bloques de código

~~~markdown
```javascript
let nombre = 'Ana';

const saludar = (nombre) => {
  return `Hola, ${nombre}!`;
};

console.log(saludar(nombre));
```
~~~

~~~javascript
let nombre = 'Ana';

const saludar = (nombre) => {
  return `Hola, ${nombre}!`;
};

console.log(saludar(nombre));
~~~

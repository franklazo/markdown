# Énfasis

```markdown
Un texto con *énfasis* va entre `*`,
con **importancia** va entre `**` y
con ***importancia y énfasis*** va entre `***`.
```

Un texto con *énfasis* va entre `*`,
con **importancia** va entre `**` y
con ***importancia y énfasis*** va entre `***`.

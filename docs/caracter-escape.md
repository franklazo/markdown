# Carácter de escape

```markdown
Es el símbolo `\`,
se usa para escribir caracteres especiales como `*` en \*énfasis\*.
```

Es el símbolo `\`,
se usa para escribir caracteres especiales como `*` en \*énfasis\*.
